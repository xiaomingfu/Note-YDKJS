#### `this` Or That?
##### Why `this`?
`this` mechanism provides a elegant way of implicitly "passing along" an object reference, leading to cleaner API design and easier re-use.
##### Confusions
The name `this` creates confusion when developers think about it too literally.
- Itself
